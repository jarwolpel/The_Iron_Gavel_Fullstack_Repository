export function CreateAccountButton() {
    return (
        <>
            <button
            onClick={() => {

            }}>
                Create Account
            </button>
        </>
    );
}